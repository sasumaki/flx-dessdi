apiVersion: eventing.knative.dev/v1beta1
kind: Trigger
metadata:
  name: event-meme-detector-display
  namespace: seldon-system

spec:
  broker: nats-broker2
  subscriber:
    ref:
      apiVersion: v1
      kind: Service
      name: event-display